# Project Assets - Styles

This directory should contain the CSS/SCSS files for your chosen admin theme.

> [!IMPORTANT]
> To use this template, you must provide your own theme assets (e.g., from Sneat or another Bootstrap-compatible theme).
> Copy the required CSS files into a `vendor/` subdirectory and ensure they match the imports in `src/styles.scss`.